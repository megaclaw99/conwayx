[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci",
  "cd backend && npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "mkdir -p backend/public",
  "cp -r dist/. backend/public/"
]

[start]
cmd = "node backend/server.js"
